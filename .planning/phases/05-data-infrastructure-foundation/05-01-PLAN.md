---
phase: 05-data-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /data/seo-metrics.json
  - /data/history/.gitkeep
  - /css/dashboard.css
  - /dashboard.html
autonomous: true
requirements:
  - VIS-01
  - AUTO-05
  - AUTO-07
must_haves:
  truths:
    - "User can open dashboard.html in browser and see structured page layout"
    - "Dashboard fetches data from /data/seo-metrics.json successfully"
    - "Dashboard displays fetched data in basic structure"
    - "JSON file contains correct schema for GSC, GEO, and alerts sections"
    - "/data/ directory exists with history subdirectory"
  artifacts:
    - path: "/data/seo-metrics.json"
      provides: "JSON data template for SEO metrics"
      contains: "gsc, geo, alerts sections"
    - path: "/data/history/.gitkeep"
      provides: "Directory structure for historical data"
    - path: "/css/dashboard.css"
      provides: "Dashboard styles inheriting theme variables"
      min_lines: 50
    - path: "/dashboard.html"
      provides: "Dashboard skeleton page"
      contains: "fetch() call to seo-metrics.json"
  key_links:
    - from: "/dashboard.html"
      to: "/data/seo-metrics.json"
      via: "fetch() in script tag"
      pattern: "fetch\\(['\"].*seo-metrics\\.json"
    - from: "/dashboard.html"
      to: "/css/dashboard.css"
      via: "link element"
      pattern: "dashboard\\.css"
    - from: "/css/dashboard.css"
      to: "/css/style.css"
      via: "CSS variable inheritance"
      pattern: "var\\(--"
---

<objective>
Create Dashboard skeleton page and JSON data infrastructure to validate the static + JSON fetch architecture. This phase establishes the foundation for SEO monitoring without backend dependencies.

**Purpose:** Verify that a pure static HTML page can fetch and display JSON data, proving the architecture works before investing in GSC API integration.

**Output:** Dashboard HTML, CSS, and JSON template files ready for data population.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-infrastructure-foundation/05-CONTEXT.md
@.planning/codebase/STACK.md

<interfaces>
<!-- Key CSS variables from existing theme. Executor should use these directly. -->

From /css/style.css:
```css
:root {
    --black: #000000;
    --black-elevated: #0a0a0a;
    --black-card: #111111;
    --orange: #ff4d00;
    --orange-glow: rgba(255, 77, 0, 0.4);
    --cyan: #00fff0;
    --cyan-glow: rgba(0, 255, 240, 0.3);
    --white: #ffffff;
    --gray: #666666;
    --radius: 12px;
    --shadow: 0 4px 24px rgba(0,0,0,0.08);
}
```

Typography:
- Headings: 'Bebas Neue', sans-serif
- Body: 'Work Sans', -apple-system, BlinkMacSystemFont, sans-serif
- h1: clamp(3rem, 8vw, 5rem)
- h2: clamp(2rem, 5vw, 3rem)
- h3: clamp(1.25rem, 3vw, 1.75rem)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JSON data template and directory structure</name>
  <files>/data/seo-metrics.json, /data/history/.gitkeep</files>
  <action>
    Create the JSON data infrastructure with proper schema structure.

    **Step 1: Create /data directory and history subdirectory**
    ```bash
    mkdir -p /Users/huxinghua/.openclaw/workspace-seo/powerstar-website/data/history
    touch /Users/huxinghua/.openclaw/workspace-seo/powerstar-website/data/history/.gitkeep
    ```

    **Step 2: Create /data/seo-metrics.json with complete schema**
    The JSON must include these sections (per CONTEXT.md decision):
    - lastUpdated: ISO timestamp
    - gsc: indexedPages, totalSubmitted, topKeywords[], clicks{}, impressions{}
    - geo: chatgpt, perplexity, claude citation status objects
    - alerts: array of alert objects

    Use placeholder/template data (no real values yet):
    ```json
    {
      "lastUpdated": "2026-04-08T00:00:00Z",
      "gsc": {
        "indexedPages": 0,
        "totalSubmitted": 0,
        "topKeywords": [],
        "clicks": {
          "last7Days": 0,
          "last30Days": 0,
          "trend": []
        },
        "impressions": {
          "last7Days": 0,
          "last30Days": 0,
          "trend": []
        }
      },
      "geo": {
        "chatgpt": { "cited": false, "lastCheck": null, "response": "" },
        "perplexity": { "cited": false, "lastCheck": null, "response": "" },
        "claude": { "cited": false, "lastCheck": null, "response": "" }
      },
      "alerts": []
    }
    ```

    This schema defines the contract for Phase 6 (GSC) and Phase 8 (GEO) implementations.
  </action>
  <verify>
    <automated>test -f /Users/huxinghua/.openclaw/workspace-seo/powerstar-website/data/seo-metrics.json && test -f /Users/huxinghua/.openclaw/workspace-seo/powerstar-website/data/history/.gitkeep && python3 -c "import json; json.load(open('/Users/huxinghua/.openclaw/workspace-seo/powerstar-website/data/seo-metrics.json'))" && echo "PASS"</automated>
  </verify>
  <done>
    - /data/seo-metrics.json exists with valid JSON
    - /data/history/.gitkeep exists
    - JSON contains gsc, geo, alerts sections with correct schema
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard CSS inheriting theme variables</name>
  <files>/css/dashboard.css</files>
  <action>
    Create dashboard stylesheet that inherits from existing theme (per CONTEXT.md decision on visual style).

    **Requirements:**
    - Import/inherit CSS variables from style.css (--black, --orange, --cyan, etc.)
    - Dark theme matching product pages
    - Responsive layout (mobile-first)
    - Card components for metric sections
    - Table styles for keyword data
    - Alert/warning styles

    **Structure:**
    ```css
    /* Dashboard Styles - Inherits from /css/style.css */

    /* Base dashboard layout */
    .dashboard { ... }

    /* Header with title */
    .dashboard-header { ... }

    /* Summary cards (indexed pages, clicks, impressions) */
    .metric-cards { ... }
    .metric-card { ... }

    /* Data tables */
    .data-table { ... }

    /* GEO status section */
    .geo-section { ... }
    .geo-status { ... }

    /* Alerts section */
    .alert-banner { ... }
    .alert-item { ... }

    /* Responsive adjustments */
    @media (max-width: 768px) { ... }
    ```

    Use CSS variables extensively for consistency:
    - Background: var(--black-elevated), var(--black-card)
    - Accent: var(--orange), var(--cyan)
    - Text: var(--white), var(--gray)
    - Shadows: var(--shadow)
  </action>
  <verify>
    <automated>test -f /Users/huxinghua/.openclaw/workspace-seo/powerstar-website/css/dashboard.css && grep -q "dashboard" /Users/huxinghua/.openclaw/workspace-seo/powerstar-website/css/dashboard.css && grep -q "var(--" /Users/huxinghua/.openclaw/workspace-seo/powerstar-website/css/dashboard.css && echo "PASS"</automated>
  </verify>
  <done>
    - /css/dashboard.css exists with min 50 lines
    - Uses CSS variables from style.css
    - Contains styles for dashboard layout, cards, tables, alerts
    - Responsive media queries included
  </done>
</task>

<task type="auto">
  <name>Task 3: Create dashboard HTML with fetch and basic render</name>
  <files>/dashboard.html</files>
  <action>
    Create dashboard skeleton page at root level (per CONTEXT.md decision).

    **HTML Structure:**
    - DOCTYPE html5, lang="en"
    - Head: title, meta charset, link to dashboard.css, Chart.js CDN (for future use)
    - Body: dark theme background
    - Main sections:
      1. Header with title and last updated timestamp
      2. Summary cards row (indexed pages, clicks, impressions - placeholder)
      3. Keywords table section (placeholder)
      4. GEO status section (placeholder)
      5. Alerts section (placeholder)

    **JavaScript Logic:**
    ```javascript
    // On DOMContentLoaded:
    // 1. Fetch /data/seo-metrics.json
    // 2. Parse JSON
    // 3. Populate summary cards with gsc.indexedPages, clicks, impressions
    // 4. Update lastUpdated timestamp
    // 5. Log success/error to console
    ```

    **Important:**
    - Dashboard is LOCAL DEVELOPMENT ONLY (per CONTEXT.md) - not deployed to production
    - Use relative path: ./data/seo-metrics.json
    - Handle fetch errors gracefully with console.error
    - Display "Loading..." state before fetch completes
    - Show "No data" if fields are empty/null

    **Note:** Per CONTEXT.md decision, verify by running local server:
    `python3 -m http.server 8080` and accessing http://localhost:8080/dashboard.html
  </action>
  <verify>
    <automated>test -f /Users/huxinghua/.openclaw/workspace-seo/powerstar-website/dashboard.html && grep -q "seo-metrics.json" /Users/huxinghua/.openclaw/workspace-seo/powerstar-website/dashboard.html && grep -q "fetch" /Users/huxinghua/.openclaw/workspace-seo/powerstar-website/dashboard.html && grep -q "dashboard.css" /Users/huxinghua/.openclaw/workspace-seo/powerstar-website/dashboard.html && echo "PASS"</automated>
  </verify>
  <done>
    - /dashboard.html exists at root level
    - Contains link to dashboard.css
    - Contains fetch() call to ./data/seo-metrics.json
    - Has basic structure: header, summary cards, placeholders for tables
    - JavaScript populates summary cards on successful fetch
    - Can be verified by opening in browser with local server
  </done>
</task>

</tasks>

<verification>
**Manual Verification (after all tasks complete):**

1. Start local server:
   ```bash
   cd /Users/huxinghua/.openclaw/workspace-seo/powerstar-website
   python3 -m http.server 8080
   ```

2. Open browser: http://localhost:8080/dashboard.html

3. Verify:
   - Page loads without errors (check browser console)
   - Dashboard displays with dark theme
   - Summary cards show data from JSON (indexedPages, clicks, impressions)
   - Last updated timestamp displays
   - Network tab shows successful fetch of seo-metrics.json
</verification>

<success_criteria>
1. User can open dashboard.html in browser and see structured page layout
2. Dashboard loads and displays data from /data/seo-metrics.json via fetch()
3. JSON template file exists with correct schema for GSC, GEO, and alerts sections
4. /data/ directory exists with proper structure for historical data files
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-infrastructure-foundation/05-01-SUMMARY.md`
</output>