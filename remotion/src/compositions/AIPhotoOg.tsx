import React from 'react';
import {ProductOg} from './ProductOg';
import {aiPhotoData} from '../data/ai-photo';

export const AIPhotoOg: React.FC = () => {
  return <ProductOg data={aiPhotoData} />;
};
